{
  "name": "crm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "functions",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=shared && npm run build --workspace=frontend && npm run build --workspace=functions",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test:rules": "echo \"test:rules: no Firestore rules tests yet (Task 2 adds them)\"",
    "test:functions": "echo \"test:functions: no Cloud Functions tests yet (Task 3 adds them)\""
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.0",
    "firebase-tools": "^13.29.1",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.67.0"
  }
}
