// Cloud Functions entry point. No callables or triggers are exported yet —
// Tasks 2-4 add the first ones. Kept as a placeholder so the emulator has a
// valid, buildable functions package to load.
export {}
